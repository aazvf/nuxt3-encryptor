{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && aws s3 sync .output/public s3://encrypt.aazvf.art/ --profile s3deploy --delete"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.11",
    "nuxt": "3.0.0-rc.10",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "crypto-js": "^4.1.1"
  }
}
